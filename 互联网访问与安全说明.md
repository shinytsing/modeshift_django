# MODESHIFT 互联网访问与安全说明

## 一、当前部署（本地虚拟机，云服务器已停用）

- **运行环境**：本地 Ubuntu 虚拟机（云服务器已过期，现用本机 Docker 部署）
- **项目**：GitHub 公开仓库 `shinytsing/modeshift_django`
- **运行方式**：`docker compose -f docker/docker-compose.prod.yml --env-file .env up -d`（在项目根目录执行）
- **服务**：PostgreSQL、Redis、Django (Gunicorn)、Nginx
- **端口**：80 (Nginx)、8000 (Django)、5432 (PostgreSQL)、6379 (Redis)

## 二、外网访问（推荐：不带端口直接访问）

### 主访问地址（无端口、HTTPS）

- **外网正式访问**：**https://shqingting.cn** 或 **https://www.shqingting.cn**（无需写端口，默认 443）
- **本机/虚拟机内**：http://192.168.27.128 或 http://127.0.0.1:8080

以上通过 **Cloudflare 隧道** 实现：流量走 Cloudflare，再经隧道到你本机 8080，不依赖家宽 80 是否开放，也不需要在地址里写 :8080。

### 使用 Cloudflare 隧道（固定域名 shqingting.cn）

1. **域名 DNS 在 Cloudflare**  
   在 Cloudflare 添加站点 **shqingting.cn**，按提示把域名的 **NS 改到 Cloudflare**（不要在阿里云等用 A 记录指 117.152.223.23，否则会变成带端口的直连）。

2. **一键创建隧道并配置 DNS（API 方式，无需浏览器登录）**  
   在项目目录执行：
   ```bash
   cd ~/modeshift_django
   # 设置 Token 后执行（Token 需有：Cloudflare Tunnel Edit + 该 Zone DNS Edit）
   export CLOUDFLARE_API_TOKEN=你的token
   ./docker/setup-tunnel-api.sh
   ```
   脚本会：创建隧道、配置 shqingting.cn / www → 本机 8080、添加 CNAME、安装并启动 cloudflared 服务。

3. **日常运维**  
   - 查看/重启隧道：`sudo systemctl status cloudflared` / `sudo systemctl restart cloudflared`  
   - 开机自启：`sudo systemctl enable cloudflared`

4. **结果**  
   用户直接访问 **https://shqingting.cn** 或 **https://www.shqingting.cn**，无需端口，证书由 Cloudflare 提供。

### 方案二：阿里云 ECS 反向代理（域名在阿里云、不带端口访问）

用一台阿里云 ECS 监听 80，把请求转发到你本机 117.152.223.23:8080，用户访问 **http://shqingting.cn** 即可（不带端口）。

**前提**：有一台阿里云 ECS（公网 IP、80 端口可用），且你家 **http://117.152.223.23:8080** 已能从外网访问（路由器 + VMware 已转发 8080）。

**1. ECS 上安装 Nginx**

- **Alibaba Cloud Linux / CentOS**：
  ```bash
  sudo yum install -y nginx
  ```
- **Ubuntu / Debian**：
  ```bash
  sudo apt update && sudo apt install -y nginx
  ```

**2. 创建反向代理配置**

在 ECS 上执行（或把本项目的 `docker/nginx-aliyun-proxy.conf` 拷到 ECS 后改名为 `shqingting.conf` 放到 `/etc/nginx/conf.d/`）：

```bash
sudo tee /etc/nginx/conf.d/shqingting.conf << 'EOF'
server {
    listen 80;
    server_name shqingting.cn www.shqingting.cn;
    location / {
        proxy_pass http://117.152.223.23:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_read_timeout 60s;
    }
}
EOF
```

**3. 检查并重载 Nginx**

```bash
sudo nginx -t && sudo systemctl reload nginx
```

**4. 阿里云 DNS**

- 在阿里云解析里，把 **shqingting.cn** 的 A 记录改为指向 **这台 ECS 的公网 IP**（不要指 117.152.223.23）。
- 同样，**www** 的 A 记录也指到 ECS 公网 IP。

**5. 安全组**

- 在阿里云 ECS 控制台，该实例的**安全组**放行 **入方向 80**（TCP）。

完成后访问 **http://shqingting.cn** 或 **http://www.shqingting.cn** 即可（不带端口）。

**一键执行（在能 SSH 到 ECS 的本机执行）**：
```bash
cd /home/shingtsing/modeshift_django   # 或你的项目路径
scp docker/setup-aliyun-proxy.sh root@47.103.143.152:/tmp/
ssh root@47.103.143.152 "chmod +x /tmp/setup-aliyun-proxy.sh && /tmp/setup-aliyun-proxy.sh"
```
输入 ECS root 密码后即可完成。最后在阿里云 DNS 把 shqingting.cn / www 的 A 记录指到 **47.103.143.152**，安全组放行 80。

---

### 备用：直连 IP（需带端口，仅调试用）

家宽通常封 80，直连必须用 **8080**，且需在路由器/VMware 做端口转发，仅作备用：

- 外网直连：http://117.152.223.23:8080 或 http://shqingting.cn:8080
- VMware：NAT 端口转发 8080 → 虚拟机 192.168.27.128:8080  
- 路由器：外网 8080 → 内网（你电脑）8080

### 4. 若遇 CSRF 报错

当前已在配置中加入 117.152.223.23 和 shqingting.cn。若仍报错，可在 `.env` 中追加：

```bash
CSRF_TRUSTED_ORIGINS=http://117.152.223.23,https://117.152.223.23,http://shqingting.cn,https://shqingting.cn
```

然后执行：`sudo docker compose -f docker/docker-compose.prod.yml --env-file .env up -d web`

## 三、已做的安全配置

- **SECRET_KEY**：由 `openssl rand -hex 32` 生成，存放在项目根目录 `.env`，不提交到 Git
- **Nginx**：反向代理、安全头（X-Frame-Options、X-Content-Type-Options、X-XSS-Protection、Referrer-Policy）
- **DEBUG=False**：生产环境关闭调试
- **ALLOWED_HOSTS=***：允许通过 IP/域名访问（可按需在 Nginx 或 Django 中收紧）
- **防火墙**：已放行 80、443、8000

## 四、建议你后续做的安全加固

1. **改掉默认密码**：在 `docker/docker-compose.prod.yml` 或 `.env` 中修改 `DB_PASSWORD`、Redis 密码（`REDIS_URL` 中的密码），并重启栈
2. **不暴露数据库/Redis 到公网**：若仅本机访问，可在 compose 中去掉 db/redis 的 `ports` 映射，或通过防火墙只允许本机
3. **HTTPS**：若有域名，在 `docker/ssl_certs/` 放入证书，并启用 `docker/nginx.production.conf` 中的 HTTPS server
4. **限流/防暴力**：可在 Nginx 或 Django 中加限流；服务器上可安装 fail2ban 防护 SSH 等

## 五、常用命令

```bash
cd ~/modeshift_django
# 启动（需先有 .env 且含 DJANGO_SECRET_KEY）
sudo docker compose -f docker/docker-compose.prod.yml --env-file .env up -d

# 停止
sudo docker compose -f docker/docker-compose.prod.yml down

# 查看日志
sudo docker compose -f docker/docker-compose.prod.yml logs -f
```
